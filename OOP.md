Class #对象名：
    def __init__(self,属性1：描述字符的类型，ect...)   eg:str int float
    def 其他属性
    

    #用法要写在属性下面 使用tab缩进
    def 用法
    
注意：用法名和属性名不能重合 否则报错

程序部分不需要缩进
----------------------------
访问限制 
对应一个内部属性 eg（面向对象编程.py)中的brand：
    是可以在属性外部使用brand = dawdjdjap之类的东西修改的

此时 我们使用__属性名 可以完成对属性内容的锁定 在外部无法使用 =  完成更改

同时
    如果开发时有修改需求 
    可以选择def一个修改方法